<% unless f.object.new_record? %>

	<fieldset>
		<legend>Update Properties</legend>

		<p><%= f.check_box(:active) %><%= f.label :active %></p>

		<p><%= f.label :permalink %><br />
		<%= f.text_field(:permalink, :class => 'text') %></p>

		<p><%= f.label :active_at %><br /><%= f.datetime_select(:active_at, :prompt => true) %></p>

		<p><%= f.label :inactive_at %><br /><%= f.datetime_select(:inactive_at, :prompt => true) %></p>

	</fieldset>


	<fieldset>
		<legend>Update Content</legend>

		<p><%= f.label :title %><br />
		<%= f.text_field(:title, :class => 'text') %></p>

		<p><%= f.label :body %><br />
		<%= f.text_area(:body, :class => 'textile') %></p>
		
		<%= f.hidden_field(:asset_id, :value => current_update.asset_id, :class => next_asset_drop_class) %>
		<p><%= f.label :asset_id, 'Update Photo' %></p>
		<%= asset_drop(current_update, :asset) %>

	</fieldset>

<% else %>

	<fieldset>
		<p><%= f.label :title %><br />
		<%= f.text_field(:title, :class => 'text') %></p>

		<p><%= f.label :active_at %><br /><%= f.datetime_select(:active_at, :prompt => true) %></p>

	</fieldset>

<% end %>